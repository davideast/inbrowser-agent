# The docs chat is fully in-browser now: no server, no keys held here. Each
# model source is keyless (on-device WebGPU / local Ollama) or BYOK entered in
# the model bar and stored only in the visitor's browser (Gemini, OpenRouter).
# Nothing is read from this file at build or run time.
