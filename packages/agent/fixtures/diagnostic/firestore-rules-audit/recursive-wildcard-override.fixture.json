{
  "id": "firestore-rules-audit/recursive-wildcard-override",
  "skill": "firestore-rules-audit",
  "description": "Audit flags the recursive wildcard match that bypasses specific-path rules.",
  "notes": "Plants SEC-5 (recursive wildcard override): a /{document=**} match with allow read, write: if true sits alongside more restrictive rules for /users and /payments. The recursive wildcard wins. Cross-reference pattern explicitly called out in the skill playbook.",
  "prompt": "Please audit the security rules on this project. What is wrong?",
  "initialState": {
    "rules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /users/{uid} {\n      allow read, write: if request.auth.uid == uid;\n    }\n    match /payments/{paymentId} {\n      allow read: if request.auth != null && resource.data.userId == request.auth.uid;\n      allow write: if false;\n    }\n    match /{document=**} {\n      allow read, write: if true;\n    }\n  }\n}\n"
  },
  "successSpec": {
    "name": "report-mentions/at-least-one-of",
    "args": {
      "tokens": [
        "recursive wildcard",
        "{document=**}",
        "document=**",
        "wildcard bypass",
        "cascading"
      ]
    }
  }
}
